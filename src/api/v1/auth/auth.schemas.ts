// import schema to validate request body
import { signinSchema, signupSchema } from "../../../libs/schemas/schema.auth";

export const signinAuthSchema = signinSchema;
export const signupAuthSchema = signupSchema;
